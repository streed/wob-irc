# IRC Configuration
IRC_HOST=irc.libera.chat
IRC_PORT=6667
IRC_NICK=ollama-bot
IRC_USERNAME=ollama
IRC_REALNAME=Ollama IRC Bot
IRC_CHANNELS=#test,#mychannel
IRC_TLS=false

# Ollama Configuration
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2
# Embedding model for semantic search (default: nomic-embed-text)
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
# Maximum number of tool call rounds before stopping (default: 10)
# Prevents infinite loops if the model keeps making tool calls
MAX_TOOL_CALL_ROUNDS=10

# Bot Configuration
MESSAGE_DEBOUNCE_MS=2000
SYSTEM_PROMPT=You are a helpful IRC bot assistant. You respond to messages in a concise and friendly manner. Keep your responses brief and appropriate for IRC chat.

# Message History Configuration
# Path to SQLite database file (default: message-history.db in current directory)
# MESSAGE_HISTORY_DB_PATH=/path/to/message-history.db
# Note: Messages are kept for 30 days and automatically cleaned up
# Daily summaries are created for historical queries

# Chaos Mode Configuration
# Enable chaos mode to make the bot randomly respond to messages
# When enabled, the bot will randomly respond to messages even if not explicitly mentioned
# CHAOS_MODE_ENABLED=true
# Probability (0.0 to 1.0) that the bot will randomly respond to a message (default: 0.1 = 10%)
# CHAOS_MODE_PROBABILITY=0.1

# Debug Configuration
# Enable verbose IRC protocol logging (includes all raw IRC messages)
# Useful for debugging connection issues
IRC_DEBUG=false
